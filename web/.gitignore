node_modules/
.next/
out/

# Secrets. The anon key is public by design, but this file is still local-only.
.env.local

# Regenerated by npm install (postinstall -> scripts/copy-ort-wasm.mjs).
public/ort/

# public/models/*.onnx is deliberately NOT ignored: no build step produces the
# model, so excluding it would deploy a site that cannot score anything.
tsconfig.tsbuildinfo

.vercel
